kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "ada-build"
spec:
  source:
    git:
      uri: "https://github.com/cokeSchlumpf/ada"
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfilePath: "Jenkinsfile"
